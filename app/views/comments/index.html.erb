<h1>Community Thoughts</h1>

<% @comments.each do |comment| %>
    <p>
        <strong><%= comment.name %></strong><br>
        <%= comment.comment %><br>
        <%= link_to 'Edit comment', comment_path(comment) %>
    </p>
    
<% end %>